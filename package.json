{
  "name": "full-stack-fastapi-template",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {"start": "concurrently --kill-others 'cd backend && poetry run uvicorn app.main:app --reload --port 8000 --host 0.0.0.0' 'cd frontend && yarn dev'"},
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^9.2.0"
  }
}
